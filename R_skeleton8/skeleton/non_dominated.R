# Implementation of the non-dominated sorting method
# relies on pareto.R
# @param costs (`data.table()`): data.table of dimension (n_points, m_costs)
# @return (`data.table()`): data.table indicating all fronts with columns of costs and additionally `front`
nDS = function(costs) {
  stop("To Implement")
}
